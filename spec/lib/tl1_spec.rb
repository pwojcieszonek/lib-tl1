# frozen_string_literal: true

RSpec.describe Lib::Tl1 do
  it "has a version number" do
    expect(Lib::Tl1::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
